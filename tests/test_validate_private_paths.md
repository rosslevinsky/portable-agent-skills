# Test Fixture: Private Paths (negative)

This file contains private paths and project-specific identifiers that should
be rejected by the validator.

It names no real private project. Every shape below is private for any workspace,
so this fixture keeps its meaning in a public clone, where the side file holding
one workspace's own identifiers does not exist.

Copy the skill from /home/alice/projects/gh/main/dotfiles/claude/skills/commit.
Ensure the file matches ~/projects/gh/main/portable-agent-skills layout.
See dotfiles/claude/skills for the original source.
Check rosslevinsky for the deployment target.
