# Test Fixture: Gate Skill Without Fallback

At the phase gate, run the `web-verify` skill to confirm the UI.

Then invoke the `diff-review` skill for an independent pass.

Done. No further instructions.
