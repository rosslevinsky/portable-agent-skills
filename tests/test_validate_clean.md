# Test Fixture: Clean

This file uses only portable language.

Search for files matching `*.md` in the project directory.
Spawn a sub-agent if supported, otherwise run sequentially.

> **Claude adapter:** Use the Agent tool with subagent_type=general-purpose.
