# Test Fixture: Unknown Skill Reference (negative)

This file references a `nonexistent-skill` skill that does not exist in
the pack. The validator must flag this.

Run the `cyw` skill to review. If the review finds issues, escalate to the
`made-up-reviewer` skill instead.
