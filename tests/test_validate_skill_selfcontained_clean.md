# Test Fixture: Self-Contained References (clean, references/-level)

See `../SKILL.md` for the skill entry point — a single `../` from a `references/` doc
reaches its own skill root and stays inside `skills/<name>/`, so it is allowed.

The planning skills maintain `plans/README.md` as a discovery index in the *user's*
project; that in-project path is legitimate and must not be flagged.
